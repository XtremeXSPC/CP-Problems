# Archived migration scripts

These scripts were used to migrate per-round directories to the centralized
build/toolchain/VS Code setup. They are kept for reference only.

If you need to re-run any of them, update the import paths
(`from migration_common import ...` now resolves locally in this directory).
