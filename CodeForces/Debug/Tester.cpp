// problem_A.cpp
#define COMPETITIVE_TEMPLATE
#include "algorithms/template.hpp"

void solve() {
  INT(n);
  debug("Input:", n);

  // La sua soluzione qui

  OUT(answer);
}

auto main() -> int {
  INT(T);
  FOR(T) solve();
  return 0;
}